#ifndef FORMATTING_H
#define FORMATTING_H

#include <iostream>
#include <string>
#include <fstream>
#include <stack>
#include <queue>
using namespace std;

void format(string FileLocIn, string FileLocOut);
int countSpaces(string s);
string tab(int n);
#endif // FORMATTING_H
